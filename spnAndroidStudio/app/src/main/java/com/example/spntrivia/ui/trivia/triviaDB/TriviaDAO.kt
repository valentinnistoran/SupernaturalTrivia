package com.example.spntrivia.ui.trivia.triviaDB

import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query

class TriviaDAO {
//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    suspend fun insert(question: TriviaQuestionsEntity)
//
//    @Query("SELECT * FROM trivia_questions WHERE difficulty = :difficulty")
//    suspend fun getQuestionsByDifficulty(difficulty: String): List<TriviaQuestionsEntity>
}